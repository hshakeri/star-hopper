# Star Hopper — Expert Review & Improvement Roadmap (June 2026)

A multi-dimensional review of the game across gameplay, story, replayability, addictivity,
physics/science, comic-book art, code health, accessibility, and audio — followed by the
first implemented improvement wave and a prioritized backlog for what's next.

## Scorecard (pre-wave)

| Dimension | Rating | One-line |
|---|---|---|
| Boot & Runtime Health | 3/10 → **fixed** | A 2-line loop guard left the canvas blank on first load — a session-ender. |
| Gameplay & Game-Feel | 8/10 → **improved** | Responsive, 5 distinct worlds; ledge grace, jump buffering, and locked-gem feedback now reduce unfair misses. |
| Storyline & Narrative | 6/10 | Coherent signal-hunt spine + Vector guide, but a faceless cadet and no payoff for the teaser worlds. |
| Non-Repeatability | 6/10 | Three real procedural systems, but flavor pools 1–2 deep and no endgame after world 5. |
| Addictivity & Progression | 7/10 | Strong predict→code→test→explain loop + badges, but no cross-session momentum. |
| Physics & Science | 7/10 → **improved** | Mature, but gravity-by-character-type taught a real misconception. |
| Comic-Book Vibe | 6/10 → **improved** | Speech balloons nail it; characters lacked ink outlines and impact pop-text. |
| Code Health | 7/10 | Safe interpreter, tests, render cache; global sprawl + duplicate navigator modules. |
| Accessibility/Mobile | 7/10 | Good touch targets + PWA; tiny fonts, animations bypass reduced-motion, unlabeled inputs. |
| Audio & Music | 6/10 → **improved** | Clean synth, but magnet/rocket/portal sounds were dead or missing. |

## Wave 1 — shipped (this pass)

All changes are zero-dependency vanilla JS, regression-tested (`node scratch/node-tests.js`,
58/58) and render-smoke-tested (`node scratch/node-smoke.js`). Cache version bumped to `coach-v48`.

1. **Boot fix (critical).** The main loop only ran `draw()` in the `playing` state, so the
   start screen showed a blank canvas. The loop now paints in the `start` state, and
   `draw()` has a start-state branch that renders the parallax starfield plus a new
   `drawStartBackdrop()` (home-planet horizon + moon, comic ink rims). — `game.js`
2. **Physics honesty.** Removed the gravity-by-character-type scaling (Rover ×0.7 / Hopper
   ×1.3) in `entities.js` and `physics.js` (sim, trajectory preview, and force-vector
   overlay) — both suits now free-fall at the same rate (Galileo). The heavy-vs-floaty feel
   is preserved through the mass-honest channels already present (`jump = jumpPower/mass`,
   `speed = engine/mass`). Two regression tests added (`test-runner.js`). Mass tooltip and a
   couple of antigravity strings reworded to avoid "cancel gravity" framing.
3. **Game-feel.** Coyote time (6-frame grace after leaving a ledge) plus a 6-frame jump buffer
   for just-before-landing inputs in `Player.update`, and wired audio: a soft landing chirp, a
   distinct rocket whoosh (replacing a probabilistic
   `playJump`), the previously-dead `playMagnet()`, and a new `playPortalUnlock()` fanfare on
   level clear. — `entities.js`, `audio.js`, `game.js`
4. **Comic pass.** Thick dark ink outlines on the Rover/Hopper silhouettes and gems; a new
   bold "impact pop-text" style (`ComicBubbles.pop`, capped at 3 concurrent) fed by the
   existing onomatopoeia pools, triggered on gem grab, enemy stomp, and mob kills. — `entities.js`, `game.js`
5. **Replayability / addictivity.** Locked-gem feedback (red pulse + wobble + throttled buzz)
   on every bump, not just the first; and the final-clear screen now points into the Daily
   Signal loop (new "TODAY'S SIGNAL" CTA) instead of dead-ending on "three worlds inbound". — `entities.js`, `game.js`, `index.html`

## Wave 2 — shipped (Depth, partial)

Cache version `coach-v49`. Tests: `node scratch/node-tests.js` → 62/62 (4 new). Verified
start screen + streak chip via headless Chrome.

1. **Deeper replay variety.** Added a `rvShiftTilesV` vertical-shift helper and new
   constraint-free geometry/target flavors so **Jupiter and Mag-Net go from 1 flavor (identical
   retries) to 3 each**, and Earth→4 / Moon→4 / Glacies→3. All preserve the required-gem count
   via the existing safety net (regression-tested over a deep attempt range). — `retry-variants.js`
2. **Gentle return-streak.** Tracks consecutive-day visits (`lastPlayedDate`/`streakCount`),
   increments on a new day, **silently resets after a gap (never shames)**, and shows a
   celebratory 🔥 chip on the start screen. Computed in `shApplyProgress` (after the saved
   fields load, since `Game.init` runs first). Consecutive-day rewards now also pop a small
   in-level streak cue, particles, and a Daily Lab CRT line when the cadet is in scene, so the
   habit loop has the same game feel as code and proof rewards. — `game.js`, `profiles.js`,
   `index.html`, `style.css`
3. **Animated planet-unlock.** Clearing a world plays a burst animation on the next galaxy-map
   node (`unlockNextPlanetNode`, respects `prefers-reduced-motion`). — `game.js`, `style.css`
4. **Persistence scaffolding.** One additive, backward-compatible profile schema for all
   Phase-2 fields (`bestClearTimes`, `masteryCleared`, `masteryMeters`, `dailySignalClears`,
   `lastPlayedDate`, `streakCount`) wired once through `shCaptureProgress`/`shApplyProgress`;
   kept OUT of the github-sync flat payload (local profile is authoritative). — `profiles.js`, `game.js`

## Backlog — still to do

### Phase 2 remainder (M effort)
- **Constraint replay tuning.**
  — Jupiter now has a rocket-rule replay and Mag-Net has a magnet-touch polarity replay, both
  with paired `getGemGateForCollectible` branches. Glacies now has a friction-target replay
  that requires the numeric `friction` variable instead of the normal spikes shortcut. Moon now
  has a strict-spring replay backed by interpreter loop telemetry, so repeat-spawned springs can
  be validated instead of inferred from object count alone. Earth now has a no-jump-power replay
  that requires stock jump and a no-mass-cut replay that keeps Hopper heavy while teaching
  alternate Agility levers, plus an engine-only replay that relocates gems to lower ledges so
  a force-only build is fair. Next step: playtest target numbers and gate copy across the full
  rotation.
- **Mastery challenges** are now gated behind full mission-gem banking, with incomplete clears
  replaying the canonical cleanup run until samples are collected. Per-world best-time tracking
  (`bestClearTimes` is ready) is surfaced on the clear screen.
- **Mastery meter + tiered badges** landed: world mastery XP now comes from discoveries,
  lab-star bests, clears, daily clears, and combat practice, with tier badges surfaced on the
  galaxy map, clear report, badge shelf, and a start-screen Mission Radar that surfaces the
  next lab quest with a direct start/accept action before kids open the Log. The Loop Engineer
  rank's Combo Amplifier now pays a visible bonus for chained new checklist progress, and the
  Mission Coach shows a labeled Observe → Predict → Code → Test → Explain loop while kids work.
  Crossing a world mastery tier now also pops `WORLD TIER!` plus the earned tier beside the
  cadet, making long-term science mastery feel like a game upgrade.
  The notebook badge shelf now previews locked rewards as `NEXT BADGE` or `LOCKED GOAL` cards
  with the upcoming mission concept and code cue, so future badges teach the next target before
  they are earned.
  The Log now adds a CRT-style Scientist Certificate path card with Play and Navigator proof
  routes, turning the printable certificate from a hidden locked button into a visible endgame
  reward that asks for one clear proof of science or navigation skill.
  The galaxy map now shows science concept chips on locked and unlocked worlds, turning future
  planets into readable lesson previews instead of blank gates.
  The start Mission Radar now also previews the next Research rank perk and XP gap, creating a
  concrete "one more experiment" target from the first screen.
  It also previews the current world mastery tier, XP, next-tier gap, and meter before launch,
  tying rescues, samples, proof runs, and remixes into one visible world ladder.
  A new start-screen 3-Star Proof row now shows whether tasks, samples, or science proof is the
  next missing mastery star before launch, making the clear-screen star rules actionable up front.
  Formula Deck now promotes the next locked card into a single experiment with one runnable
  command, so the next science/coding action is explicit even before the first card is earned.
  Locked Formula Deck cards now preview their `LEARN`, `CODE`, and `WIN` goals plus a sample
  command, making the whole collection feel like visible future progress instead of hidden
  question marks.
  The focused Formula Deck card now labels the `LEARN`, `CODE`, and `WIN` steps with
  concept-specific payoff copy, tightening each next experiment into one visible loop.
  The in-run Mission CRT now pins a Lesson Lens card above the requirements, tying the active
  science idea to one coding move while kids are still inside the level; after a prediction is
  chosen, the Lens can stage the mission scaffold directly. Its Formula target now mirrors the
  `LEARN`/`CODE`/`WIN` loop with a runnable command and `STAGE CODE` action, keeping
  the next card actionable during play. Any staged code now also flashes `CODE STAGED: press
  Enter to test` on the Mission CRT, records the staged experiment target, and pops a compact
  in-world `LAB READY` confirmation. The Mission CRT now keeps a `READY TO TEST` reminder with
  the staged command and a restage action until that exact command produces the latest science
  delta, then the What Changed card marks it as the `TESTED EXPERIMENT`, closing the gap between
  selecting a fix, running it, and reading the evidence. If the result has a follow-up, the tested
  strip can now stage the next experiment directly, and it mirrors the matching lab-chain state
  (`CHAIN READY`, `LAB CHAIN xN`, or `CHAIN PAUSED`) beside the evidence so one-variable iteration
  feels like a visible research streak instead of a hidden XP rule. The Mission CRT now also keeps
  a compact lab-chain target card in active play, so the next fresh experiment stays visible even
  after the What Changed card scrolls into the requirements list. It now also shows an in-run
  World Mastery meter with current tier, XP, next-tier gap, and the exact science sources that fill
  it, turning tasks, samples, proof, rescues, and remixes into one visible progression ladder.
  Routine world mastery gains now also pop a compact `WORLD +XP` cue beside the cadet, while
  tier crossings keep the larger `WORLD TIER!` payoff.
  The same CRT now pins a compact Star-map Signal contract with decoded story progress, the next
  science chapter, and its concrete action/reward, so narrative momentum follows the kid into
  active play instead of living only on the start screen or Log.
  Successful code runs now leave a compact What Changed science-delta card, so mass, gravity,
  engine, jump, spawned tools, and mission-stat movement become visible cause-and-effect. That
  card now also names
  the next experiment from the active mission check, turning feedback into a clear one-more-run
  loop, and includes a stageable KidCode block pulled from the mission scaffold. Repeats still
  cannot farm XP. Discovery Pulse now also shows `CHAIN NEXT` or `CHAIN PAUSED`, making the
  next valid progress target visible without rewarding repeated commands. Earned discoveries now
  also show the next lab unlock, XP gap, and progress bar directly in the Discovery Pulse, so
  the "one more experiment" target follows the kid into active play. Clear reports now also show
  the next Research rank perk, XP gap, and progress meter, tying the run result to a visible lab
  upgrade target before the replay CTA. Clear-screen replay contracts now carry an action button
  (`RETRY FOR STAR`, `RETRY FOR FORMULA`, `CHASE TIME`, `NEXT FRONTIER`, or `RUN LAUNCH PLAN`) so
  the lab report can immediately start the next learning loop instead of ending as passive text.
  The start Mission Radar now also resumes the latest saved proof's `NEXT TEST` command, giving
  returning players a one-click way to continue the lab chain from their last notebook reflection.
  Daily Signals and Frontier Challenges now carry a deterministic replay lab contract (`title`,
  science body, and sample command), so seeded challenge loops stay anchored to one visible
  coding/science experiment instead of becoming generic score runs.
  Active Daily/Frontier runs now pin that same contract inside the in-run Mission CRT with a
  `STAGE SIGNAL` action, so the replay's learning target stays visible during play.
  Running the exact Signal Lab command now pays a one-time proof reward (`SIGNAL LAB TESTED` or
  `FRONTIER LAB TESTED`) with Research XP, world mastery XP, Discovery Pulse feedback, Mission CRT
  feedback, and in-level particles, so the replay contract has a concrete payoff without becoming
  an XP farm.
  After that proof is saved, the in-run Signal Lab card flips to a `PROOF LOGGED` / `TESTED`
  state with an `EXPLAIN EVIDENCE` action that opens the Science Notebook instead of restaging a
  repeat command. That notebook handoff now seeds the evidence starter with the Signal Lab source,
  replay focus, tested command, and proof label, so the written explanation stays attached to the
  actual Daily/Frontier experiment. Saving that explanation now earns a separate once-per-proof
  `Signal Reflection Proof`, so Daily/Frontier evidence writing has its own payoff without
  colliding with the base mission reflection reward, while generic clear-report explanations clear
  stale Signal Lab context before opening the notebook. Both regular Reflection Proofs and Signal
  Reflection Proofs now also pop an in-level proof cue, particles, and a Science Notebook CRT reward
  line, making the written explanation feel like a real game action.
  Active lab chains now keep their next named milestone visible in the Research progress card and
  start Mission Radar rank preview, so a returning player can see "one more experiment to Triple
  Test" before launching the next run.
  The start-screen streak chip now previews the next daily experiment reward after today's pulse
  has been claimed, turning the gentle return streak into a visible "come back for one more test"
  target without shaming missed days.
  That streak chip now also includes a compact `DAILY` action that launches today's Daily Signal,
  so the visible return reward has a one-click experiment path instead of being passive status.
  The same chip now names today's Daily Signal focus and exposes the first sample command in the
  action title, tying the return reward to a concrete coding/science test before launch.
  It now also renders that first command as a compact code pill in the chip itself, making the
  return loop's next coding move visible without opening another panel.
  Launching from that streak chip now stages the full Daily Signal contract command in the terminal
  with the Signal Lab proof source, so the visible habit reward carries straight into a testable
  command instead of making the player retype it.
  The in-run `READY TO TEST` reminder now switches to Signal Lab proof copy for those staged
  commands, making the next Enter press read as evidence banking instead of generic code running.
  The Research rank curve now keeps the first two learning unlocks quick, then stretches the
  mid/late perk thresholds to `115/190/300` XP so Combo Amplifier, Daily Signal Lab, and Star
  Mentor stay meaningful targets across replays instead of arriving too close together.
  Replay evidence writing now answers that wider curve: regular mission reflections stay at
  `+4` Research XP, while Daily Signal, Frontier, and Dark Matter prep explanations pay
  `+5/+6/+7` once per proof source with matching world-mastery proof, making the Explain step
  a meaningful end to challenge runs without opening an XP farm.
  Cache version `coach-v248`. Next step: playtest whether the wider late-rank gaps need stronger
  Frontier rival rewards.

### Phase 3 — Frontier (L effort)
- **Build the three teased worlds** (Asteroid Forge → momentum/collisions; Dark Matter Lab →
  inferring hidden forces; Quantum Gate → branching/probability). Each needs a genuine,
  distinct physics+coding concept, not a reskin. This is the biggest retention ceiling.
- **Endless / Infinite Challenge mode** landed as Frontier Challenge: after all 6 playable
  worlds are cleared, a date-seeded tier remix unlocks from the start screen and final-clear
  CTA, with a local Frontier record strip, copyable share code, and a paste-in Class Frontier
  Board that turns same-code classmate records into a specific stars/time rival target without
  a server. Daily and Frontier signals now carry the planet's science concept plus the `3 Lab
  Stars: tasks + samples + proof` goal in start-screen copy, so the challenge loop stays
  educational before the player launches. Next step: optional online
  leaderboard UX around the same Frontier code. Imported Frontier rival targets now also appear
  inside the in-run Mission CRT during Frontier runs, showing the classmate, target stars, and
  time so the challenge follows the player into active play. Frontier clears now compare the new
  local stars/time against the imported class target and celebrate `RIVAL BEATEN` or `RIVAL MATCHED`
  in the clear report plus Mission CRT, then expose a direct `COPY WIN LINE` / `COPY MATCH LINE`
  action so classmate records become an immediate share-back loop. Clears that still trail the
  imported target now turn the next clear-screen contract into `CHASE RIVAL`, naming the classmate,
  target stars/time, and one-variable telemetry comparison. Copying a Frontier share line now also
  pays off with a `FRONTIER COPIED!` cue, particles, and Mission CRT line, so sharing feels like
  part of play rather than a plain clipboard operation. The restored star-map now also points at
  a `Dark Matter Echo` contract until the cadet clears one Frontier Challenge; the first Frontier
  record decodes that chapter and previews hidden-force inference as the next world concept. The
  locked Dark Matter Lab and Quantum Gate nodes now mirror that state on the galaxy map (`ANOMALY`,
  `ECHO DECODED`, or source trace), keeping future-world curiosity visible from the start screen.
  Once that echo is decoded, the start Mission Radar promotes a `Trace hidden force` lab quest
  that stages a Mag-Net event-rule prototype and launches the hidden-force practice world.
  Running that exact staged prototype now pays a one-time `ANOMALY TRACED` proof with Research XP,
  Mag-Net mastery, Mission CRT feedback, and Discovery Pulse chip feedback without allowing repeats
  to farm rewards. That proof now also decodes a `Hidden Force Trace` Signal Story chapter, changes
  the future-world map from `ECHO DECODED` to `SOURCE TRACED`, and returns the start Mission Radar
  to daily/replay practice instead of repeating the trace prompt. Once traced, the complete Signal
  Story contract becomes `DARK MATTER PREP`, asking kids to run Daily, Frontier, or mastery remixes
  and compare curve/speed/force evidence for the future hidden-force lab. The main start-radar
  quest now mirrors that state as `Bank curve evidence` with a `RUN PREP` action that launches a
  Frontier remix as the next evidence-gathering run. That prep tag now follows into the run's
  Mission CRT (`DARK MATTER PREP`), keeps the replay command stageable, and turns the clear report's
  next contract into another prep run instead of dropping back to generic Frontier copy. Running the
  exact prep command now pays a once-per-seed `DARK MATTER EVIDENCE` proof with stronger Research XP,
  world mastery, CRT, Discovery Pulse, and in-level reward cues.
  Banked Dark Matter evidence now hands off to `QUANTUM PREP`: the start Mission Radar stages a
  runnable conditional prototype (`player.fuel = 40` then `if player.fuel < 50`), the Quantum Gate
  teaser becomes the active map hook, and the exact branch proof pays a one-time `QUANTUM BRANCH`
  reward while unlocking the new `Branch Lab` Formula Deck card (`if condition -> branch`). After
  that branch seed is logged, the start Mission Radar advances to `QUANTUM CHANCE`, stages
  `if chance(50): player.say('path A')`, and uses the new KidCode `chance(percent)` primitive to
  teach probability as a branch that changes over repeated trials. The exact chance proof pays a
  one-time `QUANTUM CHANCE` reward, unlocks the `Probability Lab` Formula Deck card
  (`chance p -> random branch`), and moves the Quantum Gate teaser to `PROBABILITY SEED`.
  Every `chance(percent)` call now also records trials, passes, fails, and observed rate, so the
  in-run `WHAT CHANGED` panel can show a Probability row like `1/1 passed (100%)` plus the target
  chance and a reminder that more trials reveal the pattern.
  Future-lab contracts now also carry a compact source scene in the Signal Story panel and start
  radar: Vector opens the Dark Matter case file, Quantum Gate wakes after evidence is banked, and
  Hopper-Zero answers once branch/chance proofs seed the source. Each scene ties the character beat
  to one explicit science takeaway.
- **Narrative spine** now has cadet callsigns in Vector CRT transmissions, distinct Star/Hopper
  arrival quips, a final star-map hero message, a finale story chapter, and a Village Pact
  rescue chapter that turns cave behavior into a state-machine lesson. Clear reports now
  celebrate the just-decoded Signal Story chapter, preview the next chapter and science concept,
  the start Mission Radar teases the next transmission before launch, and the notebook Signal
  Story now pins a concrete next contract with its chapter reward so the narrative has a visible
  "one more run" target. The start-screen transmission preview now also names that same next
  contract and reward, so the story hook is actionable before opening the Log. New Signal Story
  chapters now also pay off in the level with a `SIGNAL DECODED!` pop, cyan/gold burst, and
  Star-map Signal monitor line, so narrative progress lands as an active-play reward. Notebook reflection
  starters pull the last code, prediction, attempt
  telemetry, latest What Changed result, and lab-chain state into the explain step. Crash-lab
  one-tap fixes now also carry a suggested next-run max-height hypothesis into the attempt log,
  so rescue-pod retries preserve the predict -> code -> test loop after failure. The Experiment
  Log now also pins a `PREDICT` / `NEXT TEST` / `EXPLAIN` lab question above attempt rows, using
  the active mission's prediction, failed result check, or reflection prompt to keep retries aimed
  at one clear science move. The same lab question now appears in the in-run Mission CRT with a
  stageable next-test action after prediction, so the loop stays visible during play rather than
  only after opening the Log. Clear reports now add an `EXPLAIN THE EVIDENCE` card with the same
  evidence starter and a direct `WRITE EXPLANATION` action into the Science Notebook, making the
  first win end in a kid-readable claim about what changed and why. Saving that explanation now
  pays a once-per-mission `Reflection Proof` Discovery Pulse, +4 Research XP, and a notebook
  history badge, so the Explain step has the same visible payoff as coding and testing without
  becoming an XP farm. Each saved proof now also carries a `NEXT TEST` card with a `STAGE NEXT TEST`
  button, turning the explanation into the next code experiment instead of ending the loop. Next step:
  deeper character banter and payoff scenes for Dark Matter Lab / Quantum Gate.
- **Successful code tweaks now reward the kid in the level, not only in the CRT.** When a KidCode
  run changes a science value, the cadet gets a compact changed-variable pop and spark burst near
  their sprite, while the mission CRT keeps the detailed "what changed / next experiment" card.
  Raw terminal runs now share the same discovery reward path as Mission Coach, so typed code can
  unlock formula cards, Research XP, and combo progress instead of only updating stats.
  New formula cards now carry a `CARD n/N` deck chip in the floating in-world card, making the
  science collection goal visible at the exact moment a kid earns a concept.
  Completing the full Formula Deck now pays a one-time `DECK MASTERED` capstone with Research XP,
  world mastery, a Mission CRT line, Discovery Pulse chip, and in-level collection cues, so the
  science-card chase has a real collection payoff without becoming repeatable.
  Correct predictions now get their own one-time hypothesis pop and burst, reinforcing the
  prediction-code-test-explain loop as a visible win condition.
  Research rank-ups from code-run discoveries, Signal Lab proofs, notebook proofs, daily streaks,
  village rescues, and mastery clears now pop `LAB RANK UP!` plus the unlocked perk beside the
  cadet, making XP progression feel like an earned game upgrade across every learning loop.
  The first real discovery now primes a `CHAIN READY!` cue, then second-or-later discoveries pop
  the lab-chain multiplier near the cadet, making the one-more-experiment combo visible without
  rewarding repeated commands.
  Fresh discovery chains across missions, Daily/Frontier proofs, and future-lab proofs now also
  pay one-time `TRIPLE TEST` / `FIVE TEST STREAK` milestones with Research XP, world mastery,
  Mission CRT feedback, Discovery Pulse chips, and in-level reward cues, turning repeated new
  experiments into a collection-style mastery loop without farming repeated commands.
  The Mission CRT lab-chain target and Discovery Pulse chain hint now preview the next named
  milestone and XP payoff before it fires, making the chain feel like a visible target rather than
  a surprise bonus.
  Coding-task completion now also pops `TASK n/N` or `TASKS DONE!` in the level and uses the CRT
  to hand off to the next fix or remaining samples.
  Lab Star gains now pop the earned categories (`TASKS`, `SAMPLES`, `PROOF`) beside the star
  reward, and the Mission CRT now mirrors each gain with the current `n/3` score plus the next
  missing star, so the mastery score explains the education goal behind the payoff.
  The next unlocked required sample now gets an in-world beacon and offscreen edge marker, so a
  successful code tweak naturally pulls the player toward the next science pickup while locked
  gates still point back to code. When code opens a gem gate, the newly collectible sample gets
  a one-time OPEN pop, sparkle burst, and expanding ring so the cause/effect moment is visible.
  Mission sample pickups now pop explicit `SAMPLE n/N` progress and an `ALL SAMPLES!` milestone,
  making collection read like evidence gathered rather than generic loot. When tasks and samples
  are both complete, the portal now fires one `PORTAL READY!` payoff at the exit, the beacon
  switches to an `EXIT` target/edge marker, and the mission CRT tells the kid to drive there or
  nudges one optional prediction/formula proof if that would turn the clear into a 3-star lab run.
  The Mission CRT also pins a compact mentor signal that connects the next formula card to a
  village specialist, a single code focus, and the upgrade payoff, so the science objective now
  reads as an in-world request instead of a detached checklist. That mentor signal now has a
  `STAGE FOCUS` action, turning the village request into a one-click experiment starter. The
  Discovery Deck's formula-focus card uses the same action, so the next collectible science card
  can be staged from both the in-run CRT and the notebook-style progress surface.
- **The D-drill now has a lab payoff instead of being only a terrain tool.** First mining a
  block on each world pays a one-time `GEOLOGY SAMPLE` pulse, and first placing a mined block pays
  a one-time `BUILD LOOP PROOF` pulse. Both feed Research XP, world mastery, the Mission CRT, and
  the Discovery Pulse while using per-world duplicate guards, so kids learn conservation of
  material and route editing without farming the same action.
- **Pet taming now pays off as a readable AI-state lesson.** The first lotion tame on each world
  creates a one-time `PET PACT` proof, and the first pet interception creates a `GUARD PROOF`
  pulse. Both feed Research XP, world mastery, the Mission CRT, and Discovery Pulse chips, making
  the hidden wild → scared → friend → guard behavior feel like a learnable system.
  Trained pets now also intercept mobs that are about to hurt villagers, with the guard proof
  switching to `pet state -> protect village`, so the pet system reinforces the village safety
  storyline instead of only protecting the cadet.
  Pet and village rescue proofs now also unlock an `AI State Lab` Formula Deck card
  (`state + event -> next state`), connecting the visible cave/pet behavior to an explicit
  coding concept and another collection target.
- **Village safety loop** is regression-tested as an AI state machine: nearby mobs send
  villagers to caves until danger clears, Survival-off releases them during daytime, and Earth
  night keeps them sheltered until morning. The frame loop now enforces those cave states after
  mobs move, starts the cave retreat in that same pass when danger closes in, night-loaded Earth
  villages start tucked into caves, and trade prompts cannot linger while villagers are sheltering.
  NPC updates and the frame-level shelter pass now share one shelter signal (`nearby mob`, `night`,
  or clear), so Survival toggles, mob proximity, and the day/night cycle cannot drift into different
  cave states.
  Hidden villagers now also treat their normal trading spot as a danger anchor, so they do not leave
  the cave while a mob is still waiting in the village even if the cave mouth itself is clear.
  In-cave villagers now show a brighter occupied cave marker, while Survival-off reports either
  `VILLAGE CLEAR` or `VILLAGE NIGHT` on the Mission CRT so the state never reads as disappearance.
  The cave marker now uses the shared shelter signal to label hidden villagers as `DANGER`, `NIGHT`,
  `WAIT`, or `SAFE`, so a villager hidden after Survival turns off at night still reads as present.
  Villagers now draw profession-specific gear overlays (wrench, code visor, map, magnet coil,
  spring, rocket pack, grip spikes, forge hammer), making the science role visible before a trade
  modal opens.
  Villager trades now pop named in-level reward cues (`CAP UP!`, `TOOL GET!`, `MAP GET!`) with
  the upgraded stat, tool, or map unlock, and the trade modal now pins a village request card that
  names the next gem target plus payoff, making gemstone spending feel like a concrete progression upgrade.
  First-time trades now also pay a one-time `TRADE PACT` / `TOOL PACT` / `MAP PACT` Discovery
  Pulse with Research XP, world mastery, Mission CRT feedback, and duplicate guards, turning
  gemstone spending into a visible sample-to-tool economy lesson instead of only a modal purchase.
  A persistent Village Trust meter now grows from first-time trades, villager rescues, and pet
  guard proofs, with compact Discovery Pulse chips (`TRUST UP`, trust title, and trust points)
  that frame the village as a relationship/state-machine system rather than one-off popups.
  Reaching the final `Village Guardian` tier now triggers a one-time `VILLAGE PACT` capstone
  with Research XP, world mastery, a Mission CRT line, Discovery Pulse chip, AI State Lab card
  unlock, and in-world reward cues, so the village relationship ladder has a clear collection
  payoff without becoming repeatable.
  Trust is saved in Cadet profiles and merged by source in backup/cloud imports, so relationship
  progress survives reloads without becoming an XP farm.
  The in-run Mission CRT now also shows a compact `VILLAGE TRUST` card with current trust tier,
  trust points, next-tier gap, and a concrete next action, so the village relationship loop becomes
  a visible target during play instead of appearing only after a reward fires.
  The start Mission Radar now mirrors that relationship ladder with current trust tier, points,
  next-tier gap, and the next relationship action before launch, making village progress part of
  the "which run should I do next?" decision.
  Galaxy-map planet nodes now also show each world's Village Trust meter beside Lab Stars and
  World Mastery, making social/story progress part of the pre-run "which world should I revisit?"
  decision.
  Clear reports now carry the Village Trust tier, trust points, next relationship milestone, and
  next action, so trades, rescues, and pet guards land as a post-run payoff alongside Lab Stars.
  Cache version `coach-v248` ships the latest behavior.

### Parallel low-risk track (code health & a11y)
- Remove dead `navigator-classic.js` (~640 LOC) — `navigator.js` solar mode is the default.
- Master volume control landed with persisted Web Audio gain. Broader
  `prefers-reduced-motion` coverage now calms starfields, start-screen drift, screen shake, and
  rave color cycling, the code/token/reflection inputs have accessible labels, and a persisted
  readable-text mode enlarges the mission, code, notebook, formula, and guide surfaces without
  changing the game canvas. Remaining: any playtest-driven contrast tweaks.

## How to run / verify locally
- Play: serve the folder (`python3 -m http.server`) and open `index.html`, or just open the file.
- Tests: `node scratch/node-tests.js` (all 8 browser suites, headless) or open `tests.html`.
- Render smoke: `node scratch/node-smoke.js` (asserts the draw paths don't throw).
