# Star Hopper — Expert Review & Improvement Roadmap (June 2026)

A multi-dimensional review of the game across gameplay, story, replayability, addictivity,
physics/science, comic-book art, code health, accessibility, and audio — followed by the
first implemented improvement wave and a prioritized backlog for what's next.

## Scorecard (pre-wave)

| Dimension | Rating | One-line |
|---|---|---|
| Boot & Runtime Health | 3/10 → **fixed** | A 2-line loop guard left the canvas blank on first load — a session-ender. |
| Gameplay & Game-Feel | 8/10 | Responsive, 5 distinct worlds; missing coyote time + locked-gem feedback. |
| Storyline & Narrative | 6/10 | Coherent signal-hunt spine + Vector guide, but a faceless cadet and no payoff for the teaser worlds. |
| Non-Repeatability | 6/10 | Three real procedural systems, but flavor pools 1–2 deep and no endgame after world 5. |
| Addictivity & Progression | 7/10 | Strong predict→code→test→explain loop + badges, but no cross-session momentum. |
| Physics & Science | 7/10 → **improved** | Mature, but gravity-by-character-type taught a real misconception. |
| Comic-Book Vibe | 6/10 → **improved** | Speech balloons nail it; characters lacked ink outlines and impact pop-text. |
| Code Health | 7/10 | Safe interpreter, tests, render cache; global sprawl + duplicate navigator modules. |
| Accessibility/Mobile | 7/10 | Good touch targets + PWA; tiny fonts, animations bypass reduced-motion, unlabeled inputs. |
| Audio & Music | 6/10 → **improved** | Clean synth, but magnet/rocket/portal sounds were dead or missing. |

## Wave 1 — shipped (this pass)

All changes are zero-dependency vanilla JS, regression-tested (`node scratch/node-tests.js`,
58/58) and render-smoke-tested (`node scratch/node-smoke.js`). Cache version bumped to `coach-v48`.

1. **Boot fix (critical).** The main loop only ran `draw()` in the `playing` state, so the
   start screen showed a blank canvas. The loop now paints in the `start` state, and
   `draw()` has a start-state branch that renders the parallax starfield plus a new
   `drawStartBackdrop()` (home-planet horizon + moon, comic ink rims). — `game.js`
2. **Physics honesty.** Removed the gravity-by-character-type scaling (Rover ×0.7 / Hopper
   ×1.3) in `entities.js` and `physics.js` (sim, trajectory preview, and force-vector
   overlay) — both suits now free-fall at the same rate (Galileo). The heavy-vs-floaty feel
   is preserved through the mass-honest channels already present (`jump = jumpPower/mass`,
   `speed = engine/mass`). Two regression tests added (`test-runner.js`). Mass tooltip and a
   couple of antigravity strings reworded to avoid "cancel gravity" framing.
3. **Game-feel.** Coyote time (6-frame grace after leaving a ledge) in `Player.update`, and
   wired audio: a soft landing chirp, a distinct rocket whoosh (replacing a probabilistic
   `playJump`), the previously-dead `playMagnet()`, and a new `playPortalUnlock()` fanfare on
   level clear. — `entities.js`, `audio.js`, `game.js`
4. **Comic pass.** Thick dark ink outlines on the Rover/Hopper silhouettes and gems; a new
   bold "impact pop-text" style (`ComicBubbles.pop`, capped at 3 concurrent) fed by the
   existing onomatopoeia pools, triggered on gem grab, enemy stomp, and mob kills. — `entities.js`, `game.js`
5. **Replayability / addictivity.** Locked-gem feedback (red pulse + wobble + throttled buzz)
   on every bump, not just the first; and the final-clear screen now points into the Daily
   Signal loop (new "TODAY'S SIGNAL" CTA) instead of dead-ending on "three worlds inbound". — `entities.js`, `game.js`, `index.html`

## Wave 2 — shipped (Depth, partial)

Cache version `coach-v49`. Tests: `node scratch/node-tests.js` → 62/62 (4 new). Verified
start screen + streak chip via headless Chrome.

1. **Deeper replay variety.** Added a `rvShiftTilesV` vertical-shift helper and new
   constraint-free geometry/target flavors so **Jupiter and Mag-Net go from 1 flavor (identical
   retries) to 3 each**, and Earth→4 / Moon→4 / Glacies→3. All preserve the required-gem count
   via the existing safety net (regression-tested over a deep attempt range). — `retry-variants.js`
2. **Gentle return-streak.** Tracks consecutive-day visits (`lastPlayedDate`/`streakCount`),
   increments on a new day, **silently resets after a gap (never shames)**, and shows a
   celebratory 🔥 chip on the start screen. Computed in `shApplyProgress` (after the saved
   fields load, since `Game.init` runs first). — `game.js`, `profiles.js`, `index.html`, `style.css`
3. **Animated planet-unlock.** Clearing a world plays a burst animation on the next galaxy-map
   node (`unlockNextPlanetNode`, respects `prefers-reduced-motion`). — `game.js`, `style.css`
4. **Persistence scaffolding.** One additive, backward-compatible profile schema for all
   Phase-2 fields (`bestClearTimes`, `masteryCleared`, `masteryMeters`, `dailySignalClears`,
   `lastPlayedDate`, `streakCount`) wired once through `shCaptureProgress`/`shApplyProgress`;
   kept OUT of the github-sync flat payload (local profile is authoritative). — `profiles.js`, `game.js`

## Backlog — still to do

### Phase 2 remainder (M effort)
- **Constraint replay tuning.**
  — Jupiter now has a rocket-rule replay and Mag-Net has a magnet-touch polarity replay, both
  with paired `getGemGateForCollectible` branches. Glacies now has a friction-target replay
  that requires the numeric `friction` variable instead of the normal spikes shortcut. Moon now
  has a strict-spring replay backed by interpreter loop telemetry, so repeat-spawned springs can
  be validated instead of inferred from object count alone. Earth now has a no-jump-power replay
  that requires stock jump and a no-mass-cut replay that keeps Hopper heavy while teaching
  alternate Agility levers, plus an engine-only replay that relocates gems to lower ledges so
  a force-only build is fair. Next step: playtest target numbers and gate copy across the full
  rotation.
- **Mastery challenges** are now gated behind full mission-gem banking, with incomplete clears
  replaying the canonical cleanup run until samples are collected. Per-world best-time tracking
  (`bestClearTimes` is ready) is surfaced on the clear screen.
- **Mastery meter + tiered badges** landed: world mastery XP now comes from discoveries,
  lab-star bests, clears, daily clears, and combat practice, with tier badges surfaced on the
  galaxy map, clear report, badge shelf, and a start-screen Mission Radar that surfaces the
  next lab quest with a direct start/accept action before kids open the Log. The Loop Engineer
  rank's Combo Amplifier now pays a visible bonus for chained new checklist progress, and the
  Mission Coach shows a labeled Observe → Predict → Code → Test → Explain loop while kids work.
  The galaxy map now shows science concept chips on locked and unlocked worlds, turning future
  planets into readable lesson previews instead of blank gates.
  The start Mission Radar now also previews the next Research rank perk and XP gap, creating a
  concrete "one more experiment" target from the first screen.
  Formula Deck now promotes the next locked card into a single experiment with one runnable
  command, so the next science/coding action is explicit even before the first card is earned.
  The in-run Mission CRT now pins a Lesson Lens card above the requirements, tying the active
  science idea to one coding move while kids are still inside the level. Successful code runs
  now leave a compact What Changed science-delta card, so mass, gravity, engine, jump, spawned
  tools, and mission-stat movement become visible cause-and-effect. That card now also names
  the next experiment from the active mission check, turning feedback into a clear one-more-run
  loop, and includes a stageable KidCode block pulled from the mission scaffold. Repeats still
  cannot farm XP. Cache version `coach-v138`. Next step: tune the XP curve after playtesting.

### Phase 3 — Frontier (L effort)
- **Build the three teased worlds** (Asteroid Forge → momentum/collisions; Dark Matter Lab →
  inferring hidden forces; Quantum Gate → branching/probability). Each needs a genuine,
  distinct physics+coding concept, not a reskin. This is the biggest retention ceiling.
- **Endless / Infinite Challenge mode** landed as Frontier Challenge: after all 6 playable
  worlds are cleared, a date-seeded tier remix unlocks from the start screen and final-clear
  CTA, with a local Frontier record strip, copyable share code, and a paste-in Class Frontier
  Board that turns same-code classmate records into a specific stars/time rival target without
  a server. Next step: optional online
  leaderboard UX around the same Frontier code.
- **Narrative spine** now has cadet callsigns in Vector CRT transmissions, distinct Star/Hopper
  arrival quips, a final star-map hero message, a finale story chapter, and a Village Pact
  rescue chapter that turns cave behavior into a state-machine lesson. Clear reports now
  preview the next Signal Story chapter and science concept, the start Mission Radar teases the
  next transmission before launch, and notebook reflection starters pull the last code,
  prediction, and attempt telemetry into the explain step. Next step:
  deeper character banter and payoff scenes for Dark Matter Lab / Quantum Gate.
- **Successful code tweaks now reward the kid in the level, not only in the CRT.** When a KidCode
  run changes a science value, the cadet gets a compact changed-variable pop and spark burst near
  their sprite, while the mission CRT keeps the detailed "what changed / next experiment" card.
  Correct predictions now get their own one-time hypothesis pop and burst, reinforcing the
  prediction-code-test-explain loop as a visible win condition.
  Real second-or-later discoveries now also pop a lab-chain cue near the cadet, making the
  one-more-experiment combo visible without rewarding repeated commands.
  Coding-task completion now also pops `TASK n/N` or `TASKS DONE!` in the level and uses the CRT
  to hand off to the next fix or remaining samples.
  The next unlocked required sample now gets an in-world beacon and offscreen edge marker, so a
  successful code tweak naturally pulls the player toward the next science pickup while locked
  gates still point back to code. When code opens a gem gate, the newly collectible sample gets
  a one-time OPEN pop, sparkle burst, and expanding ring so the cause/effect moment is visible.
  Mission sample pickups now pop explicit `SAMPLE n/N` progress and an `ALL SAMPLES!` milestone,
  making collection read like evidence gathered rather than generic loot. When tasks and samples
  are both complete, the portal now fires one `PORTAL READY!` payoff at the exit and the mission
  CRT tells the kid to drive there.
- **Village safety loop** is regression-tested as an AI state machine: nearby mobs send
  villagers to caves until danger clears, Survival-off releases them during daytime, and Earth
  night keeps them sheltered until morning. The frame loop now enforces those cave states after
  mobs move, night-loaded Earth villages start tucked into caves, and trade prompts cannot linger
  while villagers are sheltering. Cache version `coach-v138` ships the latest behavior.

### Parallel low-risk track (code health & a11y)
- Remove dead `navigator-classic.js` (~640 LOC) — `navigator.js` solar mode is the default.
- Master volume control landed with persisted Web Audio gain. Broader
  `prefers-reduced-motion` coverage now calms starfields, start-screen drift, screen shake, and
  rave color cycling, the code/token/reflection inputs have accessible labels, and a persisted
  readable-text mode enlarges the mission, code, notebook, formula, and guide surfaces without
  changing the game canvas. Remaining: any playtest-driven contrast tweaks.

## How to run / verify locally
- Play: serve the folder (`python3 -m http.server`) and open `index.html`, or just open the file.
- Tests: `node scratch/node-tests.js` (all 8 browser suites, headless) or open `tests.html`.
- Render smoke: `node scratch/node-smoke.js` (asserts the draw paths don't throw).
